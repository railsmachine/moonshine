Description:
    Generate a Moonshine plugin to provide new, reusable functionality in your manifest.

Example:
    ./script/generate moonshine_plugin iptables

    This will create the plugin structure at:
        vendor/plugins/moonshine_iptables